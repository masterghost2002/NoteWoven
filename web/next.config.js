/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'server.masterghostcloud.cloud'],
      },
      compress: true,
}

module.exports = nextConfig
